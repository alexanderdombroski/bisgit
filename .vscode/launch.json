{
  "configurations": [
    {
      "name": "Debug Tui",
      "cwd": "${workspaceFolder}",
      "request": "launch",
      "type": "node",
      "preLaunchTask": "${defaultBuildTask}",
      "console": "integratedTerminal",
      "args": ["--watch", "dist/main.js"]
    }
  ]
}
